# Dependencias
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/

# Archivos de Entorno (¡Nunca subir secretos a GitHub!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.docker

# Logs y Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Claude Code runtime (settings.local.json tiene permisos aprobados — no versionar)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Ruflo / Claude Flow runtime (estado local, no va al repo)
.claude-flow/
.swarm/
agentdb.rvf
agentdb.rvf.lock
ruvector.db
mcp-servers/

# Scripts de debugging one-off (contienen datos de prueba, no van al repo)
scripts/test-*.mjs
scripts/test-*.js

# Código legado y prototipos de diseño — propiedad privada, excluidos del repo público
_referencias_legadas/
design_handoff_edificia/

# InsForge & AI agent skills (excepto Claude Code settings que sí se versiona)
.insforge
.agent
.agents
.augment
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf

# InsForge & AI agent skills
.claude
