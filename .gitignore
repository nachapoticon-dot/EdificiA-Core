# Dependencias
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/

# Archivos de Entorno (¡Nunca subir secretos a GitHub!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs y Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Claude Code runtime (settings.local.json tiene permisos aprobados — no versionar)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Scripts de debugging one-off (contienen datos de prueba, no van al repo)
scripts/test-*.mjs
scripts/test-*.js

# Referencias legadas (binarios y scripts de auditoría, solo referencia histórica)
_referencias_legadas/

# InsForge & AI agent skills (excepto Claude Code settings que sí se versiona)
.insforge
.agent
.agents
.augment
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf
